# Chrome Link Scanner
1. Go to chrome://extensions → enable Developer Mode → Load unpacked → select this folder.
2. Open any webpage → click extension icon → see collected links.
3. Click "Scan & Check URLs" to run heuristic/VT checks.
4. Add your VirusTotal API key in Options if desired.